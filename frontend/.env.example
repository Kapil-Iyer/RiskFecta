# Copy to .env (or .env.local) — never commit either.
# Base URL of the RiskFecta FastAPI backend (Phase 2A). Not a secret — safe
# to bake into the client bundle. Defaults to the local dev server if unset.
VITE_API_BASE_URL=http://127.0.0.1:8000
