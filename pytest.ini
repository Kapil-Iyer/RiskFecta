[pytest]
markers =
    integration: local-only tests against the real, gitignored data/raw/ Bloomberg export (skipped automatically when absent, e.g. in CI).
    db: local-only tests against the real Supabase-hosted PostgreSQL database (skipped automatically when DATABASE_URL is unset, e.g. in CI).
